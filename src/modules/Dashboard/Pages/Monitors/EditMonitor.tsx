// import { useParams } from 'react-router-dom';
import AddMonitor from './AddMonitor';

const EditMonitor = () => {
	// const { monitorId } = useParams<{ monitorId: string }>();

	return <AddMonitor edit />;
};

export default EditMonitor;
